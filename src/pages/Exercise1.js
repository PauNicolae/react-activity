import React from 'react';

const Exercise1 = () => {
   return (
      <div>
         <button
            onClick={() => {
               alert('Clicked!');
            }}
         >
            Click Me
         </button>
      </div>
   );
};

export default Exercise1;
